package Day41_Inheritance;

public class Inheritance {
    /*
    - it is used to build relation between classes.

    -sub class; can be inherited variable and methods from super class.it takes.
    -super class: cannot be inherited. it gives.

    -improves re-usability
    -what can be inherited: accessible/visible methods and variables, not private
                         ***Constructor can not be  inherited.. (there is away)
                         -reason:contsructor must have the same name with the class name. when they are same,
                         when you inherite into other class the names will be different . that's not possible.names must be same



                sub-class(Child)           super-class(Parent)
    public class     Dog          extends       Animal(){
    }



     */
}
